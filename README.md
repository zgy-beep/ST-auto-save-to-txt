# 📖 SillyTavern 小说连载阅读流 (Novel Stream)

<div align="center">

![Version](https://img.shields.io/badge/Version-1.11.0-brightgreen.svg?style=flat-square)
![SillyTavern](https://img.shields.io/badge/SillyTavern-%3E%3D1.12.0-blue.svg?style=flat-square)
![Platform](https://img.shields.io/badge/Platform-Windows%20%7C%20Linux%20%7C%20Docker%20%7C%20NAS%20%7C%20Termux-orange.svg?style=flat-square)
![License](https://img.shields.io/badge/License-MIT-purple.svg?style=flat-square)

**让 AI 的每一轮回复，自然流淌成一本精彩的小说。**  
告别冰冷生硬的技术日志与聊天气泡，自动将你的角色扮演对话编排为规范、典雅的排版小说。

[✨ 核心特性](#-核心体验与亮点) • 
[⚡ 快速上手](#-极简上手指南两种模式任选) • 
[🚀 部署指南](#-详细安装与部署教程) • 
[⚙️ 设置手册](#️-连载设置手册) • 
[📱 阅读器导入](#-手机阅读器优雅食用指南) • 
[🚨 报错代码排查](#-报错代码与疑难问题速查手册-troubleshooting)

</div>

---

## 📑 目录

- [📖 核心体验与亮点](#-核心体验与亮点)
- [⚡ 极简上手指南（两种模式任选）](#-极简上手指南两种模式任选)
- [🚀 详细安装与部署教程](#-详细安装与部署教程)
  - [第一步：在 SillyTavern 中安装前端扩展](#第一步在-sillytavern-中安装前端扩展)
  - [第二步：部署服务端插件（实时自动落盘必选）](#第二步部署服务端插件实时自动落盘必选)
- [⚙️ 连载设置手册](#️-连载设置手册)
  - [章节样式与文件命名](#1-章节样式与文件命名)
  - [🏷️ 独创标签清洗引擎（白名单 / 黑名单 / 免手打检测）](#2-️-独创标签清洗引擎白名单--黑名单--免手打检测)
  - [存储路径与历史同步](#3-存储路径与历史同步)
- [📱 手机阅读器优雅食用指南](#-手机阅读器优雅食用指南)
- [🚨 报错代码与疑难问题速查手册 (Troubleshooting)](#-报错代码与疑难问题速查手册-troubleshooting)
  - [常见 HTTP 报错状态码速查表](#常见-http-报错状态码速查表)
  - [HTTP 404 - 服务端插件未安装 / 未生效](#1-http-404---未检测到服务端插件-not-found)
  - [HTTP 400 - 存储路径不合法 / 正文为空](#2-http-400---请求参数错误-bad-request)
  - [HTTP 413 - 章节篇幅超限熔断](#3-http-413---章节篇幅超出限制-payload-too-large)
  - [HTTP 500 - 文件写入权限不足 / 磁盘满](#4-http-500---写入文件失败-internal-server-error)
  - [网络层异常 - Failed to fetch / 连接拒绝](#5-网络层异常---failed-to-fetch--连接中断)
  - [非报错类的核心机制疑问解答 (FAQ)](#6-非报错类的核心机制疑问解答-faq)
- [📁 项目目录结构](#-项目目录结构)
- [📄 开源许可](#-开源许可)

---

## 📖 核心体验与亮点

- 🖋️ **智能问候语延迟入书**：新开聊天查看角色卡或调试预设时，**不会生成任何废弃文件**；只有当你发出第一句消息、真正开启剧情时，角色问候语才会作为**第 1 章**补写入书，从开头就完整，绝不漏章。
- 📚 **手机阅读器自动识别目录**：默认采用 `第 X 章 · 角色名 (原楼层: Y)` 格式，导入**微信读书、掌阅、ReadEra、多看**等主流阅读器时，**100% 自动生成可点击的树状目录**！
- 🧭 **酒馆原楼层精准溯源**：章节标题自带原始楼层标记，追溯剧情、排查漏网标签或回溯对话极其直观。
- 🔤 **原生 UTF-8 BOM 编码**：首章自动写入 UTF-8 BOM 标识与《书名》扉页，彻底杜绝老旧车载系统、老版 Windows 记事本、电子墨水屏出现“锟斤拷”乱码。
- 🔄 **智能适配“重新生成 (Regenerate)”与“划动分支 (Swipe)”**：点击酒馆重 roll 或划动分支时，插件会自动将小说末尾的最后一章替换为最新版本，绝不残留废弃废案！
- 👥 **多角色群聊 (Group Chat) 自动聚合**：智能识别群聊环境，将群内所有角色的回复统一编排进同一本群小说（`[群聊] 群名称 - 对话名.txt`），剧情连贯不撕裂。
- 🧹 **双重深度标签清洗引擎**：
  - **白名单（指定提取正文）**：如预设将正文写在 `<story>` 内，填入 `story` 即只提取该标签内容，自动丢弃外部所有杂乱元数据。
  - **黑名单（剔除干扰杂项）**：在正文中深度剥离 `<status>`、`<memory>`、`<ooc>`、`<variables>`、`<note>` 等信息卡。
  - **全自动思维链与草稿清理**：通用支持各类思考模型（DeepSeek、Claude 等），全自动剥离 `<think>`、`<cot>` 及各类反代无头闭合标签（如 `</think>`、`</analysis>`）。
  - **HTML 注释与表情包清洗**：过滤 `<!-- 世界书注释 -->` 与 `![图片](url)` 标记，回归纸质纯文字阅读体验。
- 🏷️ **会话标签一键检测器（免手打）**：面板自动扫描当前对话中 AI 实际输出过的所有标签，以标签胶囊展示，点击「白」加入白名单，点击「黑」加入黑名单，悬浮还可预览标签内的实际内容。
- 👁️ **过滤效果即时预览**：修改标签名单后，点击【查看过滤效果预览】即可弹窗对比最近回复过滤前后的完整文本与字数变化，无需等到保存落盘才排查。
- 🛡️ **未安装服务端防 404 弹窗拦截**：若尚未部署服务端，扩展会自动取消勾选连载并友好提醒，绝无频繁报错烦恼。
- 📦 **纯前端免配置一键导出**：不配置服务端插件、不碰任何黑框终端，也能直接点击【📥 导出整本小说 TXT】，浏览器端本地排版并下载！

---

## ⚡ 极简上手指南（两种模式任选）

为了照顾所有阶段的用户，插件提供两种截然不同但同样好用的运作模式：

| 模式 | 适用人群 | 使用方式 | 优点 |
| :--- | :--- | :--- | :--- |
| **模式 A：纯前端免配置导出** | 小白用户 / 云酒馆使用者 / 手机 Termux / 不想碰终端的懒人 | 安装扩展后直接聊，聊完点击面板内 **【📥 导出整本小说 TXT】** | **零门槛、免配服务端、无需重启、永不报 404**，任何设备即开即用 |
| **模式 B：后台自动实时连载** | 深度阅读用户 / NAS 同步党 / 追更爱好者 | 部署服务端插件并在 `config.yaml` 开启支持，勾选「开启小说自动连载」 | **全自动无感落盘、每轮回复自动续写、重 roll 自动替换末尾** |

---

## 🚀 详细安装与部署教程

### 第一步：在 SillyTavern 中安装前端扩展

无论使用哪种模式，都需先安装前端扩展：

1. 打开 SillyTavern，点击顶部栏的 **扩展管理图标（积木/方块图标）**。
2. 点击 **“Install Extension”（安装扩展）**，在输入框中粘贴仓库地址：
   ```text
   https://github.com/zgy-beep/ST-auto-save-to-txt
   ```
3. 点击保存安装，完成后**刷新浏览器页面**。
4. 此时顶部积木菜单中已出现 **【📖 小说连载阅读 (Novel Stream)】** 抽屉！

> [!TIP]
> 如果您只需要 **模式 A（纯前端随时导出整本 TXT）**，到这里就已经全部完成了！展开抽屉点击 **【📥 导出整本小说 TXT】** 即可。

---

### 第二步：部署服务端插件（实时自动落盘必选）

如果您希望每轮 AI 回复时，后台自动把章节悄悄写入本地文件，请执行本步以部署服务端插件：

#### 方案 1：一行命令自动部署（最简单，智能感知环境）

打开您的终端（本地电脑或服务器终端），复制对应的一行命令回车即可：

- **Windows 本机系统**（在 PowerShell 终端执行）：
  ```powershell
  irm https://cdn.jsdelivr.net/gh/zgy-beep/ST-auto-save-to-txt@main/install.ps1 | iex
  ```
- **Linux / VPS / 群晖 NAS / Docker 宿主机**（在 SSH 终端执行）：
  ```bash
  curl -fsSL https://cdn.jsdelivr.net/gh/zgy-beep/ST-auto-save-to-txt@main/install.sh | bash
  ```

> 脚本具有自适应识别能力：会自动寻找运行中的酒馆进程、定位 Docker 容器名称并自动部署进容器，完全无需手动寻址。

#### 方案 2：手动复制部署（无网络/纯离线环境）

1. 找到酒馆扩展目录中刚下载的本插件文件夹：
   - 路径通常为：`SillyTavern/public/scripts/extensions/third-party/ST-auto-save-to-txt/`（或 `data/default-user/extensions/ST-auto-save-to-txt/`）。
2. 将其中的 `plugins/auto-save` 整个文件夹，复制到 **SillyTavern 根目录的 `plugins/` 目录** 下。
3. 复制完成后，确保存在此路径：`SillyTavern/plugins/auto-save/index.js`。

#### 方案 3：启用服务端插件支持并重启酒馆

1. 打开 SillyTavern 根目录下的 **`config.yaml`** 文件。
2. 查找 `enableServerPlugins`，将其值修改为 `true`：
   ```yaml
   enableServerPlugins: true
   ```
3. **彻底重启 SillyTavern 进程**（关闭黑框终端并重新启动服务）。
4. 刷新前端网页，进入扩展面板勾选 **【开启小说自动连载】**，指示灯亮绿即表示连载服务已完美就绪！

---

## ⚙️ 连载设置手册

点击酒馆顶部积木图标，展开 **【📖 小说连载阅读】** 面板：

### 1. 章节样式与文件命名

- **开启小说自动连载**：实时连载总开关。若未部署服务端插件，系统会主动拦截并取消勾选，防止 404 弹窗。
- **连载更新时弹出轻量提示通知**：每当有新章节写入成功时，屏幕右上角弹出轻量 Toast（如 `《艾莉丝》第 12 章已自动写入`），可随时关闭。
- **章节目录风格**：
  - `第 X 章 · 角色名 (原楼层: Y)`（**强烈推荐**）：各大阅读器完美识别为目录，同时标明酒馆楼层，回溯查验极为方便。
  - `第 X 节 · 角色名`：纯净目录，不包含楼层信息。
  - `优雅分割线 (* * *)`：散文体，章节之间仅用星号隔开。
  - `纯净戏剧体 (【角色名】)`：剧本体，适合角色对话密集的场景。
- **文件命名规则**：
  - `角色名 - 对话名`（**推荐**）：每个新开的聊天都会生成独立的文件（如 `艾莉丝 - 2026-09-16.txt`），**平行世界或新开局绝不串台、绝不相互覆盖**！
  - `仅角色名`：该角色的所有对话都会无条件追加写入到同一本小说（如 `艾莉丝.txt`）。

### 2. 🏷️ 独创标签清洗引擎（白名单 / 黑名单 / 免手打检测）

玩酒馆最头疼的就是预设带有的 `<status>` 状态栏、`<think>` 思考过程、`<ooc>` 闲聊杂项。本插件提供了双重过滤机制：

```text
[原始 AI 回复]
      │
      ▼
【白名单匹配】─(未设置)────────────> [保留全部内容]
      │
      └──────(设置了标签，如 story)─> [仅提取 <story>...</story> 内部内容]
                                              │
                                              ▼
                                      【黑名单深度剔除】
                                      (剔除 status, memory, think, ooc...)
                                              │
                                              ▼
                                      [纯净优雅的小说正文]
```

- **白名单（指定正文标签）**：
  - 如果你的预设把正文包裹在固定标签里（例如 `<story>正文</story>`），在此填入 `story`。
  - **注意**：留空代表整篇全保留！若不确定预设是否有专门的正文标签，**务必留空**。
- **黑名单（排除的标签块）**：
  - 填入要剔除的标签，用半角逗号隔开，默认内置：`status,memory,details,variables,analysis,ooc,note,draft,system,log`。
  - 会把这类标签及其包含的全部子内容完整剪除。
- **会话标签检测器（免手打）**：
  - 面板会自动扫描当前聊天记录里 AI 实际吐出过的标签并列出；
  - 鼠标移动到标签上：**悬浮预览该标签内部的具体内容**；
  - 点 **「白」**：立刻加入白名单；点 **「黑」**：立刻加入黑名单；再次点击即可取消。
- **👁️ 查看过滤效果预览**：
  - 修改完名单后，点击此按钮可直接弹窗查看最新一楼 AI 回复过滤前后的对比与字数差异，彻底消除过滤配置失误的后顾之忧。

### 3. 存储路径与历史同步

- **指定保存文件夹（可选）**：
  - **留空**：默认保存在 `SillyTavern/plugins/auto-save/logs/`。
  - **自定义路径**：可填写本机电脑的绝对路径（如 `D:\MyNovels`）或相对路径（如 `novels`）。如果填写的文件夹不存在，插件会自动创建。
- **📚 同步历史到连载文件**：
  - 聊到一半才想起装插件？点击此按钮，插件会一口气将当前会话之前的全部历史楼层整理排版并写入服务端文件；后续的新回复将接着往后连载，无缝接轨！
- **📥 导出整本小说 TXT**：
  - 纯前端本地下载，即点即存，不受任何服务端插件配置限制。
- **试写一章**：
  - 模拟生成一节测试章节，快速校验写入是否正常。

---

## 📱 手机阅读器优雅食用指南

连载生成的 TXT 小说天然针对各大主流阅读软件深度调优：

```text
《艾莉丝 - 远征探索》

第 1 章 · 艾莉丝 (原楼层: 1)

　　夜幕低垂，微风拂过静谧的街角。艾莉丝合拢被风吹乱的斗篷，蓝色的眼眸在月光下泛着淡淡的光泽。
　　“你终于来了。”她轻声说道，嘴角扬起一抹似有若无的微笑。


第 2 章 · 艾莉丝 (原楼层: 3)

　　她转过身，示意你跟上她的脚步。远处的钟楼适时敲响了午夜的钟声……
```

### 主流阅读器导入推荐：
1. **微信读书**：打开微信读书 App ➔ 点击【书架】右上角【+】➔ 选择【从本地导入】➔ 选中连载生成的 `.txt` 文件。微信读书将自动识别“第 X 章”并生成层级目录，支持进度云同步与 AI 听书。
2. **ReadEra (安卓最强小说阅读器)**：将 TXT 放入手机存储目录或网盘同步文件夹，ReadEra 会自动扫描并完美展现章节目录树，且无任何广告。
3. **掌阅 / 多看阅读 / 苹果图书**：原生兼容 UTF-8 BOM 编码，打开即阅，告别文字乱码与段落挤压。

---

## 🚨 报错代码与疑难问题速查手册 (Troubleshooting)

在安装或使用过程中遇到提示或错误？请对照下方速查表，只需 1 步即可快速解决：

### 常见 HTTP 报错状态码速查表

| 报错代号 / 提示文本 | 触发场景 | 根本原因 | 1 步快速解决办法 |
| :--- | :--- | :--- | :--- |
| **HTTP 404**<br>`未检测到服务端插件 (HTTP 404)` | 勾选自动连载时被自动取消勾选，或请求被拦截 | 服务端 `plugins/auto-save` 未部署，或 `config.yaml` 未开启，或未重启酒馆 | 运行一键安装命令，确保 `config.yaml` 中 `enableServerPlugins: true`，然后**彻底重启酒馆** |
| **HTTP 400**<br>`无法直接访问您本地电脑的盘符路径` | 在 Docker / Linux 下连载或同步时报错 | 酒馆运行在服务器/容器内，但设置面板里填了 Windows 盘符（如 `D:\...`） | 将面板中的“指定保存文件夹”**清空**，或填写容器内的 Linux 真实挂载路径 |
| **HTTP 400**<br>`正文内容不能为空` | 某轮回复连载失败，报 400 | 标签过滤规则过严（如白名单填错），导致过滤后剩余文本为 0 | 打开面板检查白名单设置，不确定时请将白名单**完全留空**，点【查看过滤效果预览】排查 |
| **HTTP 413**<br>`单章节篇幅过大，超出 100KB` | 某轮回复超大，无法连载 | AI 输出陷入死循环乱码，单次体积超过 100KB（约 5 万字） | 检查该楼层是否在无限循环吐字，在酒馆内截断该回复或重新生成 |
| **HTTP 413**<br>`小说篇幅过大，超出 20MB` | 点击【同步历史连载】时报错 | 单本小说历史总字数超过 20MB（防止撑爆服务器内存熔断限制） | 直接点击面板内 **【📥 导出整本小说 TXT】**，纯前端导出不受任何体积限制 |
| **HTTP 500**<br>`EACCES / EPERM: permission denied` | 连载时面板报 500 写入失败 | Linux/Docker 运行环境没有给目标目录分配写入权限 | 在终端执行 `chmod -R 777 plugins/auto-save/logs` 赋予读写权限 |
| **HTTP 500**<br>`EBUSY: resource locked` | Windows 连载时报 500 | 目标 TXT 文件正被其他软件以独占锁形式打开（如某些老编辑器） | 关闭其他正在占用该 TXT 小说文件的编辑工具或查看器 |
| **HTTP 500**<br>`ENOSPC: no space left on device` | 连载或同步时报 500 | 服务器或电脑硬盘空间已完全占满 | 清理磁盘，腾出可用硬盘空间 |
| **网络异常**<br>`Failed to fetch` / `NetworkError` | 发送请求无响应或直接断网 | 酒馆后台 Node 进程崩溃关闭，或 Nginx/CF 反向代理未放行 API 路径 | 检查酒馆终端黑框是否正常运行；若使用反代，请确保代理了全部 `/api/` 路径 |

---

### 详细排查说明

#### 1. HTTP 404 - 未检测到服务端插件 (Not Found)
- **为什么会出现**：
  酒馆的前端扩展仅能运行在您的浏览器标签页中，而把小说文件直接写在您的电脑硬盘上，必须依靠酒馆后端的 Node.js 服务端插件。如果后端找不到 `/api/plugins/auto-save/append` 接口，就会返回 404。
- **标准解决流程**：
  1. 检查文件是否存在：进入 SillyTavern 目录，确认存在 `plugins/auto-save/index.js`。
  2. 检查主配置：打开根目录下的 `config.yaml`，确认：
     ```yaml
     enableServerPlugins: true
     ```
  3. **必须重启酒馆**：修改配置文件后，**仅刷新网页是无效的**，必须关闭运行酒馆的黑框命令行窗口，重新运行 `start.bat` 或 `node server.js`。
  4. **免服务端应急法**：如果暂时不想配置服务端，随时可以点击面板上的 **【📥 导出整本小说 TXT】** 纯前端一键导出，完全不依赖服务端。

#### 2. HTTP 400 - 请求参数错误 (Bad Request)
- **情形 A：云端容器与本地盘符冲突**
  - **报错提示**：`当前酒馆运行在云服务器(Linux/Docker)中，无法直接访问您本地电脑的盘符路径`
  - **原因**：很多用户使用的是群晖 NAS、云服务器或 1Panel Docker 搭建的酒馆，但在本地浏览器打开面板时，在“保存路径”习惯性填写了 `D:\小说`。Linux 容器内部根本无法理解 Windows 盘符。
  - **解决**：把该输入框清空即可使用默认目录；或在 Docker 容器映射卷中填写如 `/app/novels`。
- **情形 B：正文过滤后变为空白**
  - **原因**：在【指定正文标签 (白名单)】中填入了 AI 根本没用过的标签名，系统误以为整段回复全是非正文，过滤后剩余 0 个字。
  - **解决**：将白名单输入框清空（留空代表保留全部文本）。

#### 3. HTTP 413 - 章节篇幅超出限制 (Payload Too Large)
- **原因**：
  - 单章限制：`100KB`（防止 AI 幻觉死循环持续输出几万行无意义字符撑爆硬盘）。
  - 全量同步限制：`20MB`（相当于一千多万字，用于防范接口恶意大包 DOS）。
- **解决**：
  - 如果单章报错，在酒馆内检查最后一条消息是否异常过长；
  - 如果整本书确实极大，请直接点击 **【📥 导出整本小说 TXT】**，前端在浏览器直接打包生成，没有容量上限。

#### 4. HTTP 500 - 写入文件失败 (Internal Server Error)
- **权限问题 (EACCES / EPERM)**：
  - 在 Linux 或 Docker 环境下，酒馆可能是以非 root 权限（例如 `node` 账户）运行的，无法在未授权的系统目录创建或写入文件。
  - 解决方法：在服务器终端输入：
    ```bash
    chmod -R 777 /path/to/SillyTavern/plugins/auto-save
    ```
- **文件被占用 (EBUSY)**：
  - Windows 系统下，某些文本软件打开文件时会加上“写入排他锁”。关闭占用该文件的软件后重试即可。

#### 5. 网络层异常 - Failed to fetch / 连接中断
- **原因**：浏览器根本无法与酒馆后端建立通信。
- **解决**：
  1. 检查酒馆终端是否报错崩溃；
  2. 如果是通过域名、Nginx、Cloudflare 等反向代理访问，检查反向代理规则，确保代理了 `/api/` 路径并启用了 WebSocket。

---

### 6. 非报错类的核心机制疑问解答 (FAQ)

#### Q1：为什么新开聊天角色发了第一句话，文件目录里却没有创建 txt？
> **答：这是为了给您带来最好的体验而特意设计的“延迟入书机制”！**  
> 很多酒馆玩家经常频繁导入角色卡、调试打招呼设定或看卡面，如果在打开聊天窗口的第一秒就无脑落盘，您的硬盘里会堆满几百个只写了一句打招呼的几百字节废弃文件。  
> 因此，插件设计为：**当您发出第一句话正式开始对话时，系统会自动将角色的问候语作为第 1 章补写入书！** 保证连载小说从最开端就完整无缺，同时绝不产生空卡垃圾文件。

#### Q2：重新生成 (Regenerate) 或划动多条回复 (Swipe) 后，旧的废弃剧情会留在小说里吗？
> **答：绝不残留！**  
> 插件内置了智能章节末尾识别引擎。只要您在酒馆内重新生成，插件会自动精准识别并替换小说文件末尾的该章节，将其覆盖为最新生成的版本，txt 永远与酒馆当前的画面保持 100% 同步！

#### Q3：提示“内容与前文重复，已自动略过写入”，是不是漏存了？
> **答：完全不是，这正是防重复机制在发挥保护作用！**  
> 在网络波动、前端刷新或重复触发事件时，为防止同一个章节被反复写入导致小说里出现重复段落，服务端会自动比对文件末尾内容。如果末尾已包含相同章节，将自动跳过写入。

#### Q4：我想把我自己（主角/玩家）的互动对话也写进小说里，可以吗？
> **答：当然可以！**  
> 只需在扩展设置面板中勾选 **【将你的发言作为主角对白融入小说】** 即可。开启后，您的发言将作为对话或推进剧情的一部分同步编排入书。

#### Q5：微信读书导入后，为什么有些章节目录出不来？
> **答：建议将【章节目录风格】保持为默认的 `第 X 章 · 角色名 (原楼层: Y)`。**  
> 这种格式完全符合国内主流阅读器（微信读书、掌阅等）的正则目录识别算法；同时，插件生成的 TXT 带有 UTF-8 BOM 标识，导入后即可自动解析出目录，绝无乱码。

---

## 📁 项目目录结构

```text
ST-auto-save-to-txt/
├── index.js                  # 前端核心扩展代码 (UI 面板 / 状态检测 / 拦截器 / 导出逻辑)
├── style.css                 # 前端样式表 (酒馆原生自适应 / 精致抽屉 / 动态指示灯)
├── manifest.json             # SillyTavern 官方扩展清单定义
├── install.sh                # Linux / Docker / 群晖 NAS 通用一键部署脚本
├── install.ps1               # Windows PowerShell 一键部署脚本
├── README.md                 # 项目详细中英文使用与排障指南
├── .gitignore                # Git 忽略配置
├── plugins/
│   └── auto-save/            # SillyTavern 服务端插件核心目录
│       ├── index.js          # 服务端 Express 路由、排队写入与文件替换核心
│       ├── package.json      # 服务端插件清单元数据
│       └── logs/             # 默认小说连载落盘保存目录
│           └── .gitkeep      # 占位符文件
└── test/                     # 自动化测试用例套件
    ├── test-server.js        # 服务端落盘写入与替换测试
    ├── test-think.js         # 思维链深度清洗测试
    └── test-universal-tags.js # 通用标签清洗引擎测试
```

---

## 📄 开源许可

本项目遵循 [MIT License](LICENSE) 开源协议。欢迎大家提交 Issue 反馈问题或发起 Pull Request 贡献代码！
